export const enum CharCode {
    /**
	 * The `/` character.
	 */
    Slash = 47,
	/**
	 * The `:` character.
	 */
    Colon = 58,

    A = 65,
    Z = 90,
    a = 97,
    z = 122,

    /**
     * The `\` character.
     */
    Backslash = 92,
}